{% extends 'base.html.twig' %}

{% block body %}
    <h1>Debtcasedocument creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}


    <a href="{{ path('debtcasedocument_index') }}">Back to the list</a>

{% endblock %}
